#Nombre : Patrichs Inocente Valle 20132212J
#Respuesta: El codigo muestra la solucion del ejercicio 5,el uso de la funcion tst() 

#Parte a)
tst4 < function(x){
  if(x<-2) "muy negativo"
  else if(x<1) "cercano a cero"
  else if(x<3) "in [1,3)"
  else print("large")
}

function(x=0)
function(x=inf)
function(x=2)

